var username = [];
if (username.length == 0) { //Double equalto == is necessary here.
    console.log("We need to find some users!"); // Here if block will print because it's statement is matching to the given one.
}
else {
    console.log("Thanks for visiting!");
}
